plugins {
  id 'application'
  id "com.scuilion.syntastic" version "0.3.6-java7"
}

mainClassName = "ChameleonDriver"

run {
  args += 'src/main/resources/r14.txt'
  args += '15'
}

repositories {
    mavenCentral()
}

dependencies {
  compile files('libs/JBIRCH.jar')
  compile project('libs:cure')
  compile project('libs:rock')
  compile project('libs:chameleon')
  compile project('libs:chameleon_extension')
  testCompile 'junit:junit:4.12'
}

apply plugin: 'java'
  compileJava {
    options.compilerArgs << '-Xlint:unchecked'
    options.fork = true
    options.forkOptions.executable = 'javac'
}
